#include <stdio.h>
int main(){
    int r; //row
    int c; //column
    printf("enter r : \n");
    scanf("%d", &r);
    printf("enter c : \n");
    scanf("%d", &c);
    int arr[r][c];
    for (int i=0;i<r;i++){ //rows come first
        for (int j=0;j<c;j++){ //second columns
        scanf("%d", &arr[i][j]);
        }
    }
    for (int i=0;i<r;i++){
        for (int j=0;j<c;j++){
            printf("%d ",arr[i][j]);
        }
        printf("\n");
    }
    return 0;
}
